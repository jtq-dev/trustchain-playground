image:
  repository: ghcr.io/YOUR_GITHUB/trustchain-playground
  tag: "sha-DEV"
  # If you want immutable deployments, set digest and leave tag as-is:
  digest: ""

service:
  port: 80
  targetPort: 8080

resources:
  limits:
    cpu: "250m"
    memory: "256Mi"
  requests:
    cpu: "100m"
    memory: "128Mi"
